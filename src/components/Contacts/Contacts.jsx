import React from 'react';
import styles from './Contacts.module.css';

const Contacts = () => {
    return (
        <div className = {`${styles.Contacts} main_content_size`}>
            <h2>Контакты</h2>
        </div>
    )
}

export default Contacts;